# Темы ООП и C# в CanvasDrawing

## MVVM

View: `MainWindow.xaml`. ViewModel: `MainViewModel`.

## Data Binding

`ItemsControl` привязан к `VisiblePoints`.

## `ICommand`

`MouseMoveCommand` хранится во ViewModel.

## `ObservableCollection`

`VisiblePoints` обновляет интерфейс при добавлении и удалении точек.

## Динамический вывод изображения

Точки отображаются как `Ellipse` на `Canvas`.

## События

Задумка проекта связана с `MouseMoveEventArgs`, но привязка события мыши в XAML отсутствует.

## БД, async/await

Не используются.

