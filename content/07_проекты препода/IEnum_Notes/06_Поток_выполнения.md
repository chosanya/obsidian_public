# Поток выполнения IEnum

## Общая схема

```text
Program.cs
→ new Nodes(-1, 1, 5, NodesType.Equidistant)
→ foreach по eqNodes
→ GetEnumerator()
→ yield return узлов
→ eqNodes[3]
→ индексатор снова обходит коллекцию до нужного элемента
```

## Факт проверки

Временная копия проекта успешно собирается. Были только предупреждения NuGet из-за сетевой проверки уязвимостей.

