# Поток выполнения Tasks

## Общая схема

```text
Program.cs
→ await ParallelTaskCounter.GetPrimeCount()
→ Task.WhenAll
→ ParallelThreadCounter.Start()
→ Thread + Join
→ Parallel.For
→ lock
```

## Факт проверки

Временная копия проекта успешно собирается без предупреждений и ошибок.

