# Поток выполнения Threads1

## Общая схема

```text
Program.cs
→ SequentialSummator.CalcSum()
→ ParallelSummator.CalcSum()
→ создание 16 Thread
→ Start
→ CalcPartSum
→ Join
```

## Факт проверки

Временная копия проекта успешно собирается. Есть предупреждение `CS8622` из-за nullability параметра `CalcPartSum(object k)` относительно `ParameterizedThreadStart`.

